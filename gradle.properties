# The IntelliJ Platform (the target IDE) already ships the Kotlin stdlib, so do NOT add it as a
# plugin dependency - bundling a second copy would clash with the IDE's own. Compilation still sees
# the stdlib through the platform dependency on the compile classpath.
kotlin.stdlib.default.dependency=false
