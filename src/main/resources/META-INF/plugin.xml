<idea-plugin url="https://github.com/WelingtonMonteiro/multiple_run">
    <!-- Unique Marketplace id of this fork (the original Multirun owns com.khmelyuk.multirun).
         The run configuration type id ("Multirun") is intentionally kept, so run configurations
         saved with the original plugin keep working - which also means the original Multirun
         plugin must be uninstalled before installing this one. -->
    <id>io.github.welingtonmonteiro.multiplerun</id>
    <name>Multiple Run</name>
    <!-- version is injected from build.gradle (project.version) by patchPluginXml -->
    <vendor url="https://github.com/WelingtonMonteiro" email="welington.rmonteiro@gmail.com">Welington Monteiro</vendor>

    <description>
        <![CDATA[
        <p>Fork of the <a href="https://github.com/rkhmelyuk/multirun">Multirun</a> plugin by Ruslan Khmeliuk, with
        additional features such as an environment variables override applied to all grouped configurations.
        It replaces the original plugin (uninstall Multirun before installing this one); existing Multirun
        run configurations keep working.</p>

        <p>Run multiple run configurations at once: group multiple run configurations and start them in a single click.
        Not only application and test run configurations can be grouped, but other Multirun configurations can be organized
        into single run configuration.</p>

        <p>Use cases:</p>
        <ul>
            <li>Start batch of tests for multiple modules or applications</li>
            <li>Start multiple applications, like multiple servers or backend + web application</li>
        </ul>

        <p>Usage:</p>
        <ol>
            <li>Run -> Edit Configurations...</li>
            <li>Find and add a new Multiple Run configuration</li>
            <li>Add other run configurations to the Multiple Run configuration</li>
            <li>Pick options you'd like</li>
            <li>Ready to use</li>
        </ol>

        <p>Multirun + Before Launch Tasks gives even more functionality: read more at <a href="https://goo.gl/KwHvx">https://goo.gl/KwHvx</a></p>

        ]]>
    </description>

    <change-notes>
        <![CDATA[
        <p><strong>1.32.0</strong>:</p>
        <ul>
            <li>New: per-application environment file. The applications table has an "Env file (app)"
                column - point an app at its own <code>.env</code> file and it overrides the group's
                environment (variables and env file) just for that app. Empty = use the group's
                environment. Re-read on every run; relative paths resolve against the project root.</li>
        </ul>
        <p><strong>1.31.0</strong>:</p>
        <ul>
            <li>Monitor: the Ports column is now clickable - click a port to open
                <code>http://localhost:&lt;port&gt;</code> in the browser (a menu lets you pick when a
                process listens on several ports). Ports render as links.</li>
        </ul>
        <p><strong>1.30.0</strong>:</p>
        <ul>
            <li>New: import from <code>docker-compose.yml</code>. The editor's list toolbar gains an
                import button that reads a compose file and applies it to the run configurations whose
                name matches a service: <code>mem_limit</code> (and <code>deploy.resources.limits.memory</code>)
                becomes the Memory limit, <code>env_file</code> becomes an environment profile, and
                <code>depends_on</code> reorders the list (dependencies first) and adds a <code>port:</code>
                "Ready when" gate. Services without a matching run configuration are reported and skipped.</li>
        </ul>
        <p><strong>1.29.0</strong>:</p>
        <ul>
            <li>New: execution presets. Save the current On/Off selection of applications plus the
                active environment profile as a named preset, and switch between scenarios
                ("backend only", "full stack", ...) from a dropdown in the editor - no more
                re-checking boxes by hand.</li>
        </ul>
        <p><strong>1.28.1</strong>:</p>
        <ul>
            <li>Fix: the run configuration editor (Edit Configurations for a Multiple Run entry) could
                fail to open after the 1.27.2 package rename - the GUI form bound to the editor was
                accidentally removed. Restored it.</li>
        </ul>
        <p><strong>1.28.0</strong>:</p>
        <ul>
            <li>New: the Multiple Run Monitor tool window has a second "Logs" tab - an aggregated view
                of the console output of every running application, like <code>docker compose logs -f</code>.
                Each line is prefixed with its application name in a distinct color, and a filter field
                narrows the view to matching lines. The old table is now the "Processes" tab.</li>
        </ul>
        <p><strong>1.27.2</strong>:</p>
        <ul>
            <li>Internal: renamed the Java package from the original author's namespace to
                io.github.welingtonmonteiro.multiplerun. No user-facing change - existing run
                configurations keep working (the "Multirun" configuration type id is unchanged).</li>
        </ul>
        <p><strong>1.27.1</strong>:</p>
        <ul>
            <li>Fix: the "Memory limit (MB)" and "Ready when" cells no longer lose focus while
                typing (the cell editor was being closed by the dialog's validation cycle).</li>
        </ul>
        <p><strong>1.27</strong>:</p>
        <ul>
            <li>Monitor: now lists EVERY process the IDE is running - standalone (singleton) runs
                included, not only the ones started by Multiple Run. The Name column shows the origin:
                the Multiple Run icon for grouped apps, the run configuration's own icon (node, npm,
                jest, ...) for standalone ones.</li>
            <li>Monitor: an application restarted from the monitor no longer disappears from the list,
                and keeps showing its group, env profile and memory limit.</li>
            <li>Monitor: columns are resizable - drag the header edges to adjust widths.</li>
        </ul>
        <p><strong>1.26</strong>:</p>
        <ul>
            <li>Monitor: new "Mem trend" sparkline column - a mini chart with the memory history of the
                last minute per application (green/orange/red by how close to the limit it is), so leaks
                and growth trends are visible at a glance.</li>
            <li>Windows support for the monitor: memory and CPU are sampled through PowerShell
                Get-Process when ps is not available. (Ports and kill-by-port still need lsof,
                i.e. Linux/macOS.)</li>
        </ul>
        <p><strong>1.25</strong>:</p>
        <ul>
            <li>New: restart policies, docker style. "Restart application on crash" relaunches an app
                that exits with a crash code (max 3 attempts per run); stops via the stop buttons never
                trigger it. The memory limit alert threshold is now configurable (default 90%) and the
                action can be "Notify" or "Restart application" - docker-like OOM handling.</li>
        </ul>
        <p><strong>1.24</strong>:</p>
        <ul>
            <li>New: enable/disable checkbox per application - unchecked apps stay in the list but are
                not launched. Great for temporarily skipping a service without losing its settings.</li>
            <li>New: "Ready when" column, a docker-compose-like readiness gate for one-by-one starts.
                The next application only starts when the previous one is ready: port:3003 (TCP port
                open), an http(s) health URL (2xx/3xx) or log:some text (console output contains it).
                Capped at 2 minutes, then the chain continues anyway.</li>
            <li>Monitor: new "Status" column - apps with a port/http readiness condition are re-checked
                on every refresh and show healthy/down, like docker ps.</li>
            <li>Monitor: double click on a row jumps to the console tab of that application.</li>
        </ul>
        <p><strong>1.23</strong>:</p>
        <ul>
            <li>New: environment profiles. The Environment file field is now an editable dropdown that
                remembers every .env file you use - switch between environments (for example
                .env.development, .env.staging, .env.production) by picking a profile, no more retyping
                paths. The monitor shows the active profile of each running application in the new
                "Env" column.</li>
        </ul>
        <p><strong>1.22.1</strong>:</p>
        <ul>
            <li>Monitor: the selected row is no longer deselected by the automatic refresh.</li>
            <li>Monitor: fix CPU % stuck at 0.00% on Linux - the ps TIME column only has 1-second
                resolution, so the sampler now reads /proc/[pid]/stat (10 ms ticks) instead.</li>
        </ul>
        <p><strong>1.22</strong>:</p>
        <ul>
            <li>Multiple Run Monitor: Restart action per row - stop and start again only the selected
                application, the rest of the group keeps running.</li>
            <li>Multiple Run Monitor: CPU % is now instantaneous (delta between two samples, like
                docker stats) instead of the average since the process started.</li>
            <li>New: memory limit alert. When an application with a configured memory limit crosses 90%
                of it, the IDE shows a warning notification - even with the monitor closed. One alert
                per process.</li>
            <li>Multiple Run Monitor: new "Uptime" column.</li>
        </ul>
        <p><strong>1.21</strong>:</p>
        <ul>
            <li>Multiple Run Monitor: new "Ports" column showing the TCP ports each application is
                listening on (like the PORTS column of docker ps).</li>
            <li>Multiple Run Monitor: Stop and Force Kill actions per row (toolbar and right-click) -
                Force Kill sends SIGKILL to the whole process tree of the selected application.</li>
            <li>Multiple Run Monitor: "Kill Process on Port" action - type a TCP port and kill whatever
                process is listening on it (with confirmation), even processes not started by the
                plugin. Goodbye EADDRINUSE.</li>
        </ul>
        <p><strong>1.20</strong>:</p>
        <ul>
            <li>New: "Multiple Run Monitor" tool window (bottom stripe, also under Run -> Multiple Run Monitor).
                A docker-stats-like live table with every application started by Multiple Run: PID,
                memory usage vs. the configured limit (or the machine total), memory percentage and CPU.
                Measures the whole process tree of each application (npm wrapper + node child, etc.)
                and refreshes automatically every 2 seconds.</li>
        </ul>
        <p><strong>1.19</strong>:</p>
        <ul>
            <li>New: per-application memory limit. The configurations list is now a table with an editable
                "Memory limit (MB)" column - type a heap cap right next to each application and it is applied
                at launch as NODE_OPTIONS --max-old-space-size (Node.js) and JAVA_TOOL_OPTIONS -Xmx (JVM),
                the process-level analog of docker's mem_limit.</li>
        </ul>
        <p><strong>1.18</strong>:</p>
        <ul>
            <li>New: Save console logs option. Point the configuration to a folder and the console output of every
                configuration in the list is also written there as &lt;name&gt;.log, using the IDE's standard
                "save console output to file" mechanism. Relative paths resolve against the project root.</li>
        </ul>
        <p><strong>1.17</strong>:</p>
        <ul>
            <li>New: Environment file option. Point the Multiple Run configuration to a .env file (browse button
                or type the path; relative paths resolve against the project root) and its KEY=VALUE entries are
                applied to every configuration in the list. The file is re-read on every run; variables configured
                manually in the Environment variables field win over the file on conflicts.</li>
            <li>Environment override now also reaches npm/pnpm/yarn run configurations.</li>
            <li>Diagnostics: idea.log records which injection strategy each grouped configuration took
                (variable names only, never values).</li>
        </ul>
        <p><strong>1.16.2</strong>:</p>
        <ul>
            <li>Migrate off deprecated platform APIs flagged by the Marketplace verifier
                (RunnerRegistry.getRunner, ProcessAdapter, ListCellRendererWrapper), keeping the plugin
                compatible with future IDE versions. No functional changes.</li>
        </ul>
        <p><strong>1.16.1</strong>:</p>
        <ul>
            <li>Use an own id for the "Stop Multiple Run" action, fixing the "ID is already taken" PluginException
                on startup when the original Multirun plugin is installed alongside. Note: the two plugins still
                cannot run together (they share the run configuration type) - uninstall the original Multirun.</li>
        </ul>
        <p><strong>1.16</strong>:</p>
        <ul>
            <li>New: restart behavior, like the built-in Compound configuration. Running a Multirun that is already
                running now stops the processes it started before (waiting for their termination) and then starts
                again - no need to use "Stop Multirun" first. Enabled by default; can be disabled per configuration
                with the new "Restart running configurations before starting" option. Only the processes of the
                restarted Multirun are stopped - other running Multirun groups are not affected.</li>
            <li>The run configuration type is now shown as "Multiple Run" in the Add New Configuration list
                (the internal type id is unchanged, existing configurations keep working). The stop action is
                now "Stop Multiple Run".</li>
        </ul>
        <p><strong>1.15.2</strong>:</p>
        <ul>
            <li>Reference grouped run configurations by their configuration type id instead of the display name.
                Fixes matching with plugins whose type display name differs (e.g. BashSupport Pro's Shell-compatible
                configurations) and with translated IDEs (language packs). Existing configurations keep working and
                are migrated automatically on the next save.</li>
        </ul>
        <p><strong>1.15.1</strong>:</p>
        <ul>
            <li>Fix delay value corruption in locales that use a comma as the decimal separator (German, pt-BR, ...):
                the field now accepts both "." and "," and persisted values are no longer misread as tens
                (e.g. "-1.0" turning into -10).</li>
        </ul>
        <p><strong>1.15</strong>:</p>
        <ul>
            <li>New: Environment variables option on the Multirun configuration. The variables are applied to every
                configuration in the list (overriding same-named variables), including nested Multirun configurations.
                The "Include system environment variables" flag is applied to children as well.</li>
        </ul>
        <p><strong>1.14.2</strong>:</p>
        <ul>
            <li>Fix cycle detection when a Multirun contains multiple nested Multirun configurations.</li>
            <li>Fix delay value being reset to 0 in locales that use a comma as the decimal separator.</li>
            <li>Run tab updates (icon/name/pin) now happen on the EDT to avoid "must be called on EDT" errors.</li>
            <li>Build against IntelliJ Platform 2023.3 with Gradle 8.5; drop internal *Impl API usage and other cleanups.</li>
        </ul>
        <p><strong>1.14.1</strong>:</p>
        <ul>
            <li>Parsing the delay with respect to current locale.</li>
            <li>Add profiler run configurations support.</li>
        </ul>
        <p><strong>1.14</strong>:</p>
        <ul>
            <li>Add support for 2023.3 version of IntelliJ based IDEs.</li>
            <li>Fix "must be called on EDT exception"</li>
            <li>Disable delay input if "Start configuration one by one" is disabled</li>
        </ul>
        <p><strong>1.13</strong>:</p>
        <ul>
            <li>Add support for 2021.2 version of IntelliJ based IDEs.</li>
        </ul>
        <p><strong>1.12</strong>:</p>
        <ul>
            <li>Stop using some of deprecated IntelliJ Platform API.</li>
            <li>Move to Plugin SDK using Java 11.</li>
        </ul>
        <p><strong>1.11.2</strong>:</p>
        <ul>
            <li>Add plugin log icon.</li>
        </ul>
        <p><strong>1.11</strong>:</p>
        <ul>
            <li>Add support for sub-second delay.</li>
            <li>Fix waiting for delay when no more configurations to run.</li>
            <li>Add support for Coverage executor.</li>
            <li>Enable re-using tabs with successful and failure results.</li>
            <li>Fix issue when tabs couldn't be re-used after 'Stop Multirun' command.</li>
            <li>Support serial execution of the configurations (Use 'Start configurations one by one' and use negative delay)</li>
            <li>When 'Start configurations one by one' used with delay, do not wait till delay to start next configuration.</li>
        </ul>
        <p><strong>1.10</strong>:</p>
        <ul>
            <li>Update gradle-intellij-plugin and add support for IntellijIDEA 2019.1</li>
        </ul>
        <p><strong>1.9</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2018.1</li>
        </ul>
        <p><strong>1.8</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2017.3</li>
        </ul>
        <p><strong>1.7</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2017.2</li>
        </ul>
        <p><strong>1.6</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2017.1</li>
        </ul>
        <p><strong>1.5</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2016.3</li>
        </ul>
        <p><strong>1.4</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 2016.2 EAP</li>
        </ul>
        <p><strong>1.3</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 16</li>
        </ul>
        <p><strong>1.1</strong>:</p>
        <ul>
            <li>Support for IntellijIDEA 15</li>
        </ul>
        <p><strong>1.0.1</strong>:</p>
        <ul>
            <li>Adding support for JRebel run configurations (thanks olmoser!)</li>
        </ul>
        <p><strong>1.0</strong>:</p>
        <ul>
            <li>It works stable for almost 2 years for me. So I feel free to say it's 1.0 version. Woohoo!!</li>
            <li>Fixed API issues with using Multirun from IntellijIDEA 14.1</li>
            <li>Change shortcut to kill Multirun to Control + Alt + K on OS X</li>
        </ul>
        <p><strong>0.9</strong>:</p>
        <ul>
            <li>Fixed API issues with using Multirun from IntellijIDEA 14 (thanks derkork!)</li>
            <li>Change shortcut to kill Multirun to Shift + Alt + K to avoid conflict with Git plugin</li>
            <li>Delay can be added before starting configurations in one-by-one mode</li>
        </ul>
        <p><strong>0.8</strong>:</p>
        <ul>
            <li>Fixed API issues with using Multirun from IntellijIDEA 13 (thanks jgindin!)</li>
        </ul>
        <p><strong>0.7</strong>:</p>
        <ul>
            <li>Enable Run Another Configuration option for Before Launch tasks if not enabled yet</li>
        </ul>
        <p><strong>0.6.1</strong>:</p>
        <ul>
            <li>Fix API issue when using Multirun in IDEA 12.0.x</li>
        </ul>
        <p><strong>0.6</strong>:</p>
        <ul>
            <li>Enabled Debug for Multirun configuration</li>
            <li>Fix issue with starting composite Multirun configuration</li>
        </ul>
        <p><strong>0.5</strong>:</p>
        <ul>
            <li>
                Fixed reported and found issues
                <ul>
                    <li>NPE during process start</li>
                    <li>circular reference issue during loading Multirun configurations</li>
                    <li>killing composite/parallel Multirun configurations</li>
                </ul>
            </li>
        </ul>
        <p><strong>0.4</strong>:</p>
        <ul>
            <li>Mark the tab for failed process</li>
            <li>Added option to close tabs for successfully completed run configurations</li>
            <li>Better description for configuration options</li>
            <li>Do not re-use the tab of failed run configuration</li>
        </ul>
        <p><strong>0.3</strong>:</p>
        <ul>
            <li>Stop running configurations using menu Run -> Stop Multirun or shortcut Ctrl + Shift + K</li>
            <li>Improve Multirun configuration edit form</li>
            <li>Pin tabs when the re-use tabs option is disabled</li>
            <li>Mark running configurations with *</li>
            <li>Change in default configuration: tabs re-use is disabled, starting configurations one-by-one is enabled</li>
            <li>Improved plugin description</li>
        </ul>
        <p><strong>0.2</strong>:</p>
        <ul>
            <li>Enable Multirun for PyCharm, RubyMine, PhpStorm, WebStorm and AppCode</li>
        </ul>
        <p><strong>0.1</strong>:</p>
        <ul>
            <li>Multirun run configuration option to group multiple run configurations and run them in a single click</li>
            <li>Group multiple Multirun configurations in single master Multirun configuration to start everything</li>
            <li>Basic configuration options:
                <ul>
                    <li>start configurations one-by-one or in parallel</li>
                    <li>re-use the tabs or run each configuration in separate tab</li>
                </ul>
            </li>
        </ul>
        ]]>
    </change-notes>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/build_number_ranges.html for description -->
    <idea-version since-build="233.0000"/>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>

    <actions>
        <!-- id must stay in sync with StopRunningMultirunConfigurationsAction.ACTION_ID;
             namespaced to avoid colliding with the original Multirun plugin's action id -->
        <action id="MultipleRun.StopRunning" class="io.github.welingtonmonteiro.multiplerun.StopRunningMultirunConfigurationsAction"
                text="Stop Multiple Run" description="Stop running Multiple Run configurations" icon="/actions/suspend.png">
            <add-to-group group-id="RunMenu" relative-to-action="Stop" anchor="after"/>
            <keyboard-shortcut keymap="$default" first-keystroke="shift alt K"/>
            <keyboard-shortcut keymap="Mac OS X" first-keystroke="control alt K"/>
        </action>
        <action id="MultipleRun.ShowMonitor" class="io.github.welingtonmonteiro.multiplerun.ShowMultirunMonitorAction"
                text="Multiple Run Monitor" description="Show memory and CPU usage of the applications started by Multiple Run">
            <add-to-group group-id="RunMenu" relative-to-action="MultipleRun.StopRunning" anchor="after"/>
        </action>
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <configurationType implementation="io.github.welingtonmonteiro.multiplerun.MultirunConfigurationType"/>
        <programRunner implementation="io.github.welingtonmonteiro.multiplerun.MultirunRunner"/>
        <!-- docker-stats-like monitor for the processes started by the plugin -->
        <toolWindow id="Multiple Run Monitor" anchor="bottom" canCloseContents="false"
                    icon="AllIcons.Toolwindows.ToolWindowRun"
                    factoryClass="io.github.welingtonmonteiro.multiplerun.ui.MultirunMonitorToolWindowFactory"/>
        <!-- balloon warnings raised by the memory limit watcher -->
        <notificationGroup id="Multiple Run" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
