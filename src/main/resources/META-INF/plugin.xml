<idea-plugin url="https://github.com/PixelC0d3/lanes">
    <!-- Unique Marketplace id of this fork (the original Multirun owns com.khmelyuk.multirun).
         The run configuration type id is "Lanes" (changed from "Multirun" before this fork
         had any real users), so it can never collide with the original plugin's own type id if
         both are ever installed side by side; the original Multirun plugin should still be
         uninstalled before installing this one. -->
    <id>io.github.pixelcodes.lanes</id>
    <name>Lanes</name>
    <!-- version is injected from build.gradle (project.version) by patchPluginXml -->
    <vendor url="https://github.com/WelingtonMonteiro" email="welington.rmonteiro@gmail.com">Welington Monteiro</vendor>

    <description>
        <![CDATA[
        <!-- The Marketplace renders this description as standalone HTML - it has no access to the
             plugin's own .zip, so the image below is pulled from GitHub's raw content CDN instead
             of a local path. It only resolves once this branch is merged into mainline. -->
        <!-- width is capped (not 100%) so the banner stays modest in the IDE's install panel, where
             the description also renders - a full-width hero looked oversized there. -->
        <p><img src="https://raw.githubusercontent.com/PixelC0d3/lanes/mainline/brand/banner.png" alt="Lanes" width="420"/></p>

        <p><b>Run, monitor and manage multiple run configurations at once.</b> Group your apps, servers,
        microservices or tests and start them with a single click - one by one with "ready when"
        (port / HTTP / log) checks when you need startup ordering. Then watch everything live in a
        docker-stats-style monitor: memory, CPU, listening ports, uptime and health, with memory-leak
        analysis, per-process memory charts and aggregated logs.</p>

        <p>Manage environments without leaving the IDE: load <b>.env</b> environment files per app or group,
        switch between environment profiles on the fly, and <b>import a docker-compose</b> file to create run
        configurations. Not only application and test configurations can be grouped - other Lanes
        configurations can be nested into a single one.</p>

        <p>Fork of the <a href="https://github.com/rkhmelyuk/multirun">Multirun</a> plugin by Ruslan Khmeliuk.
        It replaces the original plugin (uninstall Multirun before installing this one).</p>

        <p>Use cases:</p>
        <ul>
            <li>Start batch of tests for multiple modules or applications</li>
            <li>Start multiple applications, like multiple servers or backend + web application</li>
        </ul>

        <p>Usage:</p>
        <ol>
            <li>Run -> Edit Configurations...</li>
            <li>Find and add a new Lanes configuration</li>
            <li>Add other run configurations to the Lanes configuration</li>
            <li>Pick options you'd like</li>
            <li>Ready to use</li>
        </ol>

        <p>Lanes + Before Launch Tasks gives even more functionality: read more at <a href="https://goo.gl/KwHvx">https://goo.gl/KwHvx</a></p>

        <p>If this plugin saves you time, consider <a href="https://buymeacoffee.com/welingtonmonteiro">buying me a coffee</a> ☕</p>

        ]]>
    </description>

    <change-notes>
        <![CDATA[
        <p><strong>1.0.4</strong>:</p>
        <ul>
            <li>The <code>.env</code> picker now renders like a combo box, matching the run
                configuration selector next to it (icon, active profile name, dropdown arrow) -
                picking a profile updates the name shown right there, no separate popup.</li>
        </ul>
        <p><strong>1.0.3</strong>:</p>
        <ul>
            <li>Fixes 1.0.2 (which never actually rendered): a toolbar button next to the run
                configuration selector now lets you pick which <code>.env</code> profile the
                selected Lanes group or native Node run configuration runs with next, once it has
                2 or more saved profiles - no modal, persists as the active profile.</li>
        </ul>
        <p><strong>1.0.1</strong>:</p>
        <ul>
            <li>Internal: the project moved to the <a href="https://github.com/PixelC0d3">PixelC0d3</a>
                GitHub organization. The plugin id is now <code>io.github.pixelcodes.lanes</code> and
                the Kotlin package moved accordingly.</li>
            <li>No functional change; Welington Monteiro remains the plugin's maintainer/vendor.</li>
        </ul>
        <p><strong>1.0</strong>: Initial release</p>

        <p><b>Grouping</b></p>
        <ul>
            <li>Group any number of run configurations into a single <b>Lanes</b> configuration and
                start them with one click.</li>
            <li><b>Nesting / composite configurations</b>: a Lanes configuration can contain other
                Lanes configurations, so you can build a "master" configuration that starts several
                groups at once - with loop protection (detects and prevents cycles).</li>
        </ul>

        <p><b>Execution modes</b></p>
        <ul>
            <li><b>Parallel</b> or <b>one by one</b> (the next configuration starts only after the
                previous one has started - useful when "Before launch" tasks would otherwise
                collide).</li>
            <li><b>Enable/disable per application</b> - an On checkbox per row; unchecked apps stay
                in the list (keeping their settings) but are not launched.</li>
            <li><b>Ready when</b> (docker-compose-style depends_on): with one-by-one, the next
                application only starts once the previous one is actually ready -
                <code>port:3003</code>, an http(s) health URL (2xx/3xx), or <code>log:some text</code>
                (console output contains it). Capped at 2 minutes, then the chain continues anyway.
                Also feeds the monitor's Status column (healthy/down, re-checked every refresh).</li>
            <li><b>Delay between configurations</b> (one-by-one only): 0 starts immediately, a
                positive value waits up to N seconds (with a countdown), a negative value waits for
                the previous process to complete first (serial execution). Locale-aware (accepts
                "," or "." as the decimal separator).</li>
        </ul>

        <p><b>Environment variables &amp; .env files</b></p>
        <ul>
            <li>Define environment variables directly on the Lanes configuration - applied to
                every child configuration, overriding same-named variables, and propagating through
                nested Lanes configurations too.</li>
            <li><b>Environment file with profiles</b>: point the Environment file field at one or
                more .env files (multi-select browse) and switch between them from a dropdown - no
                retyping paths. Re-read on every run; the configuration's own Environment variables
                field still wins on conflicts.</li>
            <li><b>Per-application env file override</b>: an Env file (app) column lets a single app
                use its own .env file instead of the group's.</li>
            <li><b>Native Play/Debug support</b>: the same .env-profile loading is available
                directly on Node-based run configurations (Node.js, npm/pnpm/yarn, Karma, Jest,
                Mocha) - not just inside a Lanes group. A switch button on the run toolbar restarts
                that same run with the chosen profile. Optional module: only active in IDEs with the
                JavaScript plugin; uninstalling the plugin removes it cleanly.</li>
            <li>Works with any configuration type that exposes environment variables (Node.js, npm,
                Java Application, ...); other types run unchanged.</li>
        </ul>

        <p><b>Execution presets</b></p>
        <ul>
            <li>Save the current On/Off selection of applications plus the active environment
                profile as a named preset, and switch between scenarios ("backend only", "full
                stack", ...) from a dropdown - no more re-checking boxes by hand.</li>
        </ul>

        <p><b>Import from docker-compose.yml</b></p>
        <ul>
            <li>Reads a compose file and applies it to run configurations already in the list whose
                name matches a service: <code>mem_limit</code> (and
                <code>deploy.resources.limits.memory</code>) becomes the Memory limit,
                <code>env_file</code> becomes an environment profile, <code>depends_on</code>
                reorders the list and adds a <code>port:&lt;published&gt;</code> Ready when gate.
                Services without a matching configuration are reported and skipped - it never
                creates one.</li>
        </ul>

        <p><b>Per-application memory limit</b></p>
        <ul>
            <li>An editable Memory limit (MB) column applies a heap cap at launch
                (<code>NODE_OPTIONS --max-old-space-size</code> for Node.js,
                <code>JAVA_TOOL_OPTIONS -Xmx</code> for the JVM) - the process-level analog of
                Docker's <code>mem_limit</code>.</li>
        </ul>

        <p><b>Lanes Monitor (docker-stats-style process table)</b></p>
        <ul>
            <li>Lists every process the IDE is running - apps started by Lanes and standalone
                (singleton) runs - with PID, ports, uptime, status, memory usage/limit, memory %,
                memory trend and CPU %. Each application is measured as its whole process tree,
                refreshed automatically every 2 seconds. A distinct "configured" icon marks every
                saved Lanes instance everywhere the platform shows it.</li>
            <li>Env column shows the active profile - click to view the variables actually loaded
                (with a name filter and a mask-values toggle for screen sharing) - for grouped apps
                and standalone runs alike.</li>
            <li>Ports are clickable (opens the URL in the browser); Kill Process on Port... stops
                whatever is listening on a port, even processes the plugin didn't start.</li>
            <li>Mem trend sparkline (color-coded by proximity to the limit) opens a full-session
                memory chart with labeled axes, CSV export, and a leak-analysis tab (growth rate,
                projected growth/hour, R², a per-process breakdown, and a text export).</li>
            <li>Restart / Stop / Force Kill per row or in batch (multi-select survives the automatic
                refresh); Restart All and Restart Unhealthy toolbar actions; Switch Environment
                per-row or group-wide, preserving each app's executor.</li>
            <li>Show/Hide columns picker; Aggregated Logs tab merges every running app's console
                into one color-coded stream, with an app selector, composable filters
                (substring/regex, show/hide, case-sensitive, minimum log level) and an ANSI toggle.</li>
            <li>Shows a branded empty-state illustration when nothing is running.</li>
            <li>Windows support via PowerShell Get-Process for memory/CPU (Ports and Kill-by-Port
                need lsof, Linux/macOS only).</li>
        </ul>

        <p><b>Status bar widget</b></p>
        <ul>
            <li>A compact indicator shows how many applications Lanes is running, their combined
                memory and how many are unhealthy. Click it to open the Lanes Monitor; it hides
                itself when nothing is running.</li>
        </ul>

        <p><b>Restart policies &amp; alerts (docker style)</b></p>
        <ul>
            <li><b>Restart application on crash</b> - relaunches an app that exits with a crash code
                (max 3 attempts per run); intentional stops never trigger it.</li>
            <li><b>Crash notification with Restart</b> - a one-click Restart button when crash-restart
                is off or exhausted.</li>
            <li><b>Unhealthy alert</b> - an app whose readiness condition stays down for 3 consecutive
                checks is reported unhealthy with a Restart button; re-arms on recovery.</li>
            <li><b>Sustained CPU alert</b> - a configurable CPU % threshold held for 3 consecutive
                checks raises a notification with Restart.</li>
            <li><b>Memory limit alert</b> - configurable threshold (default 90%) with a Notify or
                Restart application action, docker-like OOM handling.</li>
        </ul>

        <p><b>Console tab handling</b></p>
        <ul>
            <li>Mark the tab of a failed configuration; optionally close tabs of successfully
                completed ones; reuse tabs for succeeded and/or failed runs.</li>
            <li>Save console logs to folder - writes each configuration's output as a log file via
                the IDE's standard mechanism.</li>
        </ul>

        <p><b>Restarting and stopping</b></p>
        <ul>
            <li><b>Restart on rerun</b> (default on) - running a Lanes configuration that's already
                running stops its own processes first, then starts again; other running Lanes groups
                are untouched. Can be disabled per configuration.</li>
            <li><b>Stop Lanes</b> action stops everything the plugin started, available from
                Run -&gt; Stop Lanes and via Shift+Alt+K (macOS: Control+Alt+K).</li>
        </ul>

        <p><b>Supported executors</b></p>
        <ul>
            <li>Run, Debug, Run with Coverage, Profiler, and JRebel (Run and Debug).</li>
        </ul>
        ]]>
    </change-notes>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/build_number_ranges.html for description -->
    <idea-version since-build="233.0000"/>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>
    <!-- OPTIONAL: only loaded when the host IDE has the JavaScript plugin (WebStorm, IDEA Ultimate,
         ...). Adds an "Env Files" tab to Node-based run configurations (Node.js, npm, Karma, Jest,
         Mocha) so a standalone Play/Debug can load .env files too - the same feature Lanes
         groups already have. In IDEs without JavaScript the module is skipped and the core plugin
         works unchanged. See META-INF/lanes-nodejs.xml. -->
    <depends optional="true" config-file="lanes-nodejs.xml">JavaScript</depends>

    <actions>
        <!-- id must stay in sync with StopRunningLanesConfigurationsAction.ACTION_ID;
             namespaced to avoid colliding with the original Multirun plugin's action id -->
        <action id="Lanes.StopRunning" class="io.github.pixelcodes.lanes.StopRunningLanesConfigurationsAction"
                text="Stop Lanes" description="Stop running Lanes configurations"
                icon="io.github.pixelcodes.lanes.LanesIcons.StopAll">
            <add-to-group group-id="RunMenu" relative-to-action="Stop" anchor="after"/>
            <keyboard-shortcut keymap="$default" first-keystroke="shift alt K"/>
            <keyboard-shortcut keymap="Mac OS X" first-keystroke="control alt K"/>
        </action>
        <action id="Lanes.ShowMonitor" class="io.github.pixelcodes.lanes.ShowLanesMonitorAction"
                text="Lanes Monitor" description="Show memory and CPU usage of the applications started by Lanes">
            <add-to-group group-id="RunMenu" relative-to-action="Lanes.StopRunning" anchor="after"/>
        </action>
        <!-- toolbar button next to the run configuration selector: pick which .env profile the
             selected Lanes group runs with next, once it has 2+ saved profiles -->
        <action id="Lanes.PickEnvProfile" class="io.github.pixelcodes.lanes.LanesEnvProfileToolbarAction"
                text="Env" description="Pick which .env profile the selected Lanes group runs with next"
                icon="io.github.pixelcodes.lanes.LanesIcons.EnvVariables">
            <add-to-group group-id="ToolbarRunGroup" anchor="before" relative-to-action="RunnerActions"/>
            <add-to-group group-id="MainToolbarRight" anchor="before" relative-to-action="NewUiRunWidget"/>
        </action>
    </actions>

    <extensions defaultExtensionNs="com.intellij">
        <configurationType implementation="io.github.pixelcodes.lanes.LanesConfigurationType"/>
        <programRunner implementation="io.github.pixelcodes.lanes.LanesRunner"/>
        <!-- docker-stats-like monitor for the processes started by the plugin -->
        <toolWindow id="Lanes Monitor" anchor="bottom" canCloseContents="false"
                    icon="io.github.pixelcodes.lanes.LanesIcons.OpenDashboard"
                    factoryClass="io.github.pixelcodes.lanes.ui.LanesMonitorToolWindowFactory"/>
        <!-- status bar widget: running apps, total memory and unhealthy count (click opens the monitor) -->
        <statusBarWidgetFactory id="Lanes.Monitor.Widget"
                                implementation="io.github.pixelcodes.lanes.ui.LanesStatusBarWidgetFactory"
                                order="last"/>
        <!-- balloon warnings raised by the memory limit watcher -->
        <notificationGroup id="Lanes" displayType="BALLOON"/>
        <!-- keeps the running-apps count badge on the monitor tool window in sync with process start/stop -->
        <postStartupActivity implementation="io.github.pixelcodes.lanes.ui.LanesMonitorBadgeActivity"/>
        <!-- clears any stale cached icon so every saved Lanes instance shows the "configured" icon -->
        <postStartupActivity implementation="io.github.pixelcodes.lanes.LanesConfiguredIconRefreshActivity"/>
    </extensions>
</idea-plugin>
